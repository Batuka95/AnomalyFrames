[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "emuinput"
version = "0.1.0"
description = "ADB-orchestrated uinput bridge for Android emulators"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["emuinput"]